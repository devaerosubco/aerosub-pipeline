# Copy to .env for local dev. Values come from `npx supabase status`.
# In production these are set as Cloudflare Pages environment variables.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<local anon key — run: npx supabase status>
